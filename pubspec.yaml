name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.25+25  #250208 2130

environment:
  sdk: '>=2.19.4 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.0.3
  cloud_firestore:
  firebase_core:
  firebase_auth:
  firebase_storage:
  firebase:
  flutter_svg:
  flutter_riverpod:
  dart_midi_pro: ^1.0.4
  flutter_midi_pro: ^3.1.4
  audioplayers:
  dart_melty_soundfont:
  flame:
  flame_forge2d:
  url_launcher:
  fl_chart: ^0.64.0
  english_words: ^4.0.0
  provider: ^6.0.0
  shared_preferences:
  go_router:
  google_mobile_ads:
  font_awesome_flutter:
  intl:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  fonts:
    - family: makinas4
      fonts:
        - asset: assets/fonts/makinas4/Makinas-4-Flat.otf
          weight: 300
  uses-material-design: true
  assets:
    - Images/
